# `F-C2-DraftsToInbox` — on E's own phone. **It passes.**

> **E's device look, 2026-09-22, taken by E.** Three frames plus E's verdict in chat. This is the
> look `F-C2` was owed since it merged on 2026-09-20, and it discharges it.

**Environment:** E's iPhone 15 Pro, **iOS 27.0**, the LIVE Firebase project and E's real account
(so every count below is E's own data), `main` @ `4917955` installed wirelessly, Reduce Motion
**OFF**, app force-quit before the first frame. Frames are E's own screenshots.

**Verified paths:** `26/27 path: run on E's phone (RM off)`. **No RM-on pass was owed** and none
was asked for — this block added no reduced site; the capsule's motion is `F-C1`'s and untouched
(that pass PASSED on 2026-09-20).

## What passed

**1. A composer closed on typed text files it, and says so.** `00-` is the capsule on Today
reading **"Kept in your inbox"** over the draft's first line, with **↗ Reopen** as its control —
E's round-2 words, on a phone. The Captures badge reads 25, one more than the 24 it read minutes
earlier in `../undo-capsule-device/07-`: the filed draft, counted.

**2. The draft is a real capture, not a private scratch buffer.** `01-` is the Journal timeline
with *"Quick question, what's that going on?"* at 4:10 am, tagged **`captured`** — the same feed
any note capture appears in, because a filed draft IS an ordinary `.note` capture through the
existing seam (which is why the block owed no `firestore.rules` change).

**3. Reopen opens THAT capture, not merely the tab.** `02-` is the Capture Inbox with the draft on
the triage card — *"Note · under an hour old · Quick question, what's that going on?"* — at the top
of **To triage (25)**. E's Step 0 answer 1 (*"Open it in the inbox"*), confirmed on device.

**4. Task detail: swipe-back restored, and the edit saves itself.** **E's verdict, in chat:**
*"Open a task, edit a field, swipe back from the left edge → no 'Discard changes?', and the edit is
saved when you go back in. — Also passes"*. **There is no frame for this one**, and that is
recorded rather than papered over: the claim rests on E's word plus the simulator evidence in
`../drafts-to-inbox/` (`09-` to `11-`, all three appearances), where the same journey is driven and
asserted. A frame of an alert NOT appearing is the one thing easiest to take on trust and hardest
to photograph on someone else's phone.

## What this look does NOT cover

**The radius cap still has no device evidence**, and it belongs to `F-C1` rather than here — see
`../undo-capsule-device/README.md`. E's larger-text look was at the top of the STANDARD range with
"Larger Accessibility Sizes" off, and the cap only differs from a plain capsule once the layout
stacks, which needs an accessibility size. The simulator render covers it
(`../drafts-to-inbox/04-note-closed-capsule-kept-in-your-inbox-AX3.jpg`). No block owes it.

## The frames

| file | what it proves |
|---|---|
| `00-the-capsule-kept-in-your-inbox-reopen` | The capsule on Today: **"Kept in your inbox"**, the draft's first line beneath it, **↗ Reopen**. The badge at 25 — one more than four minutes earlier — is the filed draft counted. |
| `01-the-draft-in-the-journal-timeline-as-captured` | The same draft in the Journal's "Everything" feed at 4:10 am, marked **`captured`**: it is an ordinary note capture, not a special draft type. |
| `02-reopen-landed-on-that-capture` | **Reopen's destination.** The Capture Inbox with that exact draft on the triage card, "under an hour old", at the top of To triage (25) — the capture, not just the tab. |
