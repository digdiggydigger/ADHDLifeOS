# `F-E3-OneCardToday` — Today's one card

**IN PROGRESS (2026-09-26).** The session that built the block stopped at ~71% context before the
harness frames were filed; `handoff/START-HERE-adhd-audit-arc-E3-finish.md` owes the rest of this
folder (`TodayOneCardRenderUITests`, light / dark / AX3 on iPhone 17 Pro · iOS 27.0, emulator).

## The two boards E answered from (2026-09-26)

Rendered with the REAL tokens and the shipped `TodayFilledButtonStyle` / `TodayTintedButtonStyle`
at 402pt (iPhone 17 Pro width) by a throwaway `ImageRenderer` probe, deleted after (board `59`'s
precedent). The next-step line is drawn as `Text`: `ImageRenderer` cannot draw a UIKit
`TextField` and paints a yellow "no" bar in its place — a probe artifact, not the app.

| file | what it settled |
|---|---|
| `00-E3-Q1-close-layout.jpg` | `F-E1`'s gain line "Close it — makes today count" in H1's half-width Close. A: side by side, 354pt card, two lines (three at xxxLarge, where the render caught the pair going RAGGED — 84pt beside 48pt — fixed with one-height pairing, `buttons.md › Style`). B: stacked, 410pt. **E: "A · Side by side".** |
| `00-E3-Q2-done-line.jpg` | The done line once a daily goal is set. **E: "B · '3 of 5 done today'"** — with the ring gone, the only place a chosen goal can be seen. |
