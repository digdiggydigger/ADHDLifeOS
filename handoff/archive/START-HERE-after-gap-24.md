# START HERE — after the 24pt gap: two looks owed, then E's next call

*Written 2026-09-17 ~13:30 BST at E's request to close the session and continue fresh. **This is a
disposable pointer. Archive it when you write your successor.** Its predecessor,
`archive/START-HERE-looks-on-three-blocks.md`, is spent.*

## Read these first

1. `claudecode.md`, then `CLAUDE.md`'s Architecture notes, §2, §5 and §7 — NOT `docs/`.
2. `handoff/OPEN-ITEMS-REGISTER.md` — the fifty-eighth edition's **State** block.
3. `TODO-CLAUDE-CODE.md`, the last section: the three blocks (all `[x]`, with E's narrowing of
   `F-FanHoldsCelebration`) and `F-FurnitureGap24` (one `[ ]`: the device look).

## State

`main` @ `173ba7d` (or the close-out merge after it), clean. Suite 3,053 / 0, SwiftLint 0 / 824.
**The phone:** read the register's "Device" line — it says which SHA is installed. **The profile
minted 2026-09-13 expires 2026-09-17T19:25:02Z (20:25 BST)**; if E reports the app will not open, the
fix is a device REBUILD after that time (`-allowProvisioningUpdates` then re-issues), not a bug hunt.

The Firebase emulator was started this session; a fresh session restarts it before any baseline
(memory `emulator-freshness`).

## Owed by E (ask in ONE message, only once the build is confirmed on the phone)

1. **The 24pt gap** — the collapsed bar and the disc 24pt above the tab bar, portrait AND landscape,
   alone and under a Confirm card; and the **Journal tab**: the disc against the "One line about
   today" field, now ~5.5pt off its centre (it was 2.5 at 32). E chose this cost knowingly.
2. **Reduce Motion ON** — open and close the fan with a card up: the × jumps (by design, §7.2); the
   cards must still FADE, not cut. This is the regression check nothing on the sim has proved.

`F-FanHoldsCelebration` has no ordinary look (see the register); do not ask for one unless E wants to
set up a background daily goal.

## Lessons the next session should not re-pay (memory `sprint-seed-harness-traps`)

A seeded PAUSED sprint drives `SprintBarFurnitureUITests`; the timer bar's container identifier
overrides its children's (find Pause by label); kill a host screenshot poller when xcodebuild exits,
never `wait` on it; kill a wedged xcodebuild by PID, never `pkill -f` a pattern your own shell matches.

## Carried, unchanged

- Frame 08's observation (the pushed-up disc over the hero's *Start another session*) — observation only.
- Phase D's one unlooked-at delta: the schedule summary line's dimness in dark on 27.0.
- The colour-scheme arc: HELD.
